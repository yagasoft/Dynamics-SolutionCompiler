# Acceptance Ledger

This ledger records what is currently proven in the `.NET 10` repository, what is still partial, and what remains missing.

## Evidence Scale

- `done` - proven and documented.
- `partial` - useful proof exists, but boundaries remain.
- `missing` - not yet proven in a durable way.

## Ledger

| Area | Status | Evidence | Notes |
| --- | --- | --- | --- |
| Docs spine | done | `README.md`, `docs/roadmap.md`, `docs/architecture.md`, `docs/backlog/backlog.md`, `docs/acceptance/ledger.md`, `docs/threads/current.md` | Repository now has a stable planning and handoff surface. |
| B-010 phase lock | done | `docs/roadmap.md`, `docs/backlog/backlog.md`, `docs/threads/current.md`, `docs/acceptance/ledger.md`, `fixtures/skill-corpus/references/component-coverage-matrix.md` | The authoritative current phase is now the full completion program for all remaining incomplete touched families. The planning docs now treat Waves 1-7, the program-wide completion rule, and the three allowed end states as the source of truth until the program is complete. |
| B-010 completion program | done | `README.md`, `docs/roadmap.md`, `docs/backlog/backlog.md`, `docs/threads/current.md`, `docs/acceptance/ledger.md`, `fixtures/skill-corpus/references/component-coverage-matrix.md`, `scratch/b010-wave7-maximal-proof/final-proof-self-contained/post-reimport/reverse-from-export/intent-spec/reverse-generation-report.json` | The completion program now lands every touched family in one explicit end state. Waves 1, 2, 4, 5, and 7 clear the live export-backed rebuild bar; Wave 3 `CanvasApp` plus `EntityAnalyticsConfiguration` clear it too; and compact AI is now an explicit permanent boundary rather than a hidden partial. |
| Generator status accounting | done | `README.md`, `fixtures/skill-corpus/references/component-coverage-matrix.md`, `docs/roadmap.md`, `docs/backlog/backlog.md`, `docs/threads/current.md` | Docs now distinguish `Intent author`, `Reverse from tracked-source`, and `Rebuild from intent` readiness so "touched" no longer implies "fully generatable." The current classes are `full rebuildable`, `authorable but partial`, `source/readback only`, and `source-first / permanent best-effort`, and the active `B-010` program now requires every touched family to end in one explicit class. |
| Exhaustive owner-family accounting | done | `fixtures/skill-corpus/references/solutioncomponent-componenttype-inventory.json`, `fixtures/skill-corpus/references/component-catalog.md`, `fixtures/skill-corpus/references/component-coverage-matrix.md`, `docs/backlog/backlog.md`, `docs/roadmap.md`, `docs/threads/current.md`, `README.md`, `tests/DataverseSolutionCompiler.UnitTests/ComponentUniverseInventoryTests.cs` | The repo now accounts for the official current `solutioncomponent.componenttype` universe plus the local-observed `80` `App Module` supplement. Every owner family is explicit in the coverage matrix, every subordinate or internal-only type is documented instead of silently omitted, no owner-family rows remain `planned`, and the inventory validation now guards that closure directly. |
| Public-facing repository onboarding | done | `README.md`, `docs/backlog/backlog.md`, `docs/roadmap.md`, `docs/threads/current.md`, `tests/DataverseSolutionCompiler.UnitTests/RepositoryDocumentationTests.cs` | The repo now has a portable GitHub-facing entrypoint instead of a workspace-specific baton README. The public README uses relative links, quick-start workflow examples, a concise repo map, and explicit proof/boundary language, and regression tests now guard against drifting back to workspace-specific absolute paths. |
| Code-first SDK registration ingestion and plugin deployment proof | done | `src/DataverseSolutionCompiler.Readers.Code/CodeFirstSdkRegistrationReader.cs`, `src/DataverseSolutionCompiler.Apply/DotNetCodeAssetBuilder.cs`, `src/DataverseSolutionCompiler.Apply/WebApiApplyExecutor.cs`, `src/DataverseSolutionCompiler.Agent/AgentOrchestrator.cs`, `tests/DataverseSolutionCompiler.UnitTests/CodeFirstSdkRegistrationReaderTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/CodeAssetBuilderTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/AgentOrchestratorTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/CliApplicationTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/WebApiApplyExecutorTests.cs`, `tests/DataverseSolutionCompiler.E2ETests/CodeFirstPluginWorkflowProofTests.cs`, `fixtures/skill-corpus/examples/seed-code-plugin-classic`, `fixtures/skill-corpus/examples/seed-code-plugin-package` | The compiler now has a narrow code-first path for the known DBM-style raw C# registration pattern. It detects `CodeFirstSdkRegistration`, projects code into the existing plug-in families, preserves the code-first evidence through tracked-source and reverse-generated `sourceBackedArtifacts[]`, stages signed `.dll` or `.nupkg` assets outside the source tree, and deploys them through `apply-dev` or `publish` without inventing a new component family. The classic and package seeds now cover the supported parser/build/workflow/readback/drift story, and the environment-gated E2E harness can verify the two proof markers when a real Dataverse URL is configured. |
| Wider code-first registration and custom workflow activity phase A | done | `src/DataverseSolutionCompiler.Readers.Code/CodeFirstSdkRegistrationReader.Imperative.cs`, `src/DataverseSolutionCompiler.Domain/Model/ArtifactPropertyKeys.cs`, `src/DataverseSolutionCompiler.Emitters.TrackedSource/TrackedSourceEmitter.cs`, `src/DataverseSolutionCompiler.Diff/StableOverlapDriftComparer.cs`, `src/DataverseSolutionCompiler.Apply/DotNetCodeAssetBuilder.cs`, `src/DataverseSolutionCompiler.Agent/AgentOrchestrator.cs`, `tests/DataverseSolutionCompiler.UnitTests/CodeFirstSdkRegistrationReaderTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/CodeAssetBuilderTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/CliApplicationTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/WebApiLiveSnapshotProviderTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/StableOverlapDriftComparerTests.cs`, `tests/DataverseSolutionCompiler.IntegrationTests/TrackedSourceEmitterIntegrationTests.cs`, `fixtures/skill-corpus/examples/seed-code-plugin-imperative`, `fixtures/skill-corpus/examples/seed-code-workflow-activity-classic`, `fixtures/skill-corpus/examples/seed-code-workflow-activity-package` | The compiler now supports the seeded imperative DBM helper registration shape in addition to the first narrow code-first plug-in parser shape. It also preserves custom workflow activities as `PluginType` subtype `customWorkflowActivity` through tracked-source, reverse-generated `sourceBackedArtifacts[]`, live readback, and stable-overlap, while keeping the owner-level `Workflow` lane closed as a separate boundary. Classic assembly staging and deployment now work for that subtype, and plug-in package deployment fails early with an explicit diagnostic instead of silently pretending workflow extensions are package-supported. |
| Helper-based code-first registration and custom workflow activity phase B | done | `src/DataverseSolutionCompiler.Readers.Code/CodeFirstSdkRegistrationReader.cs`, `src/DataverseSolutionCompiler.Readers.Code/CodeFirstSdkRegistrationReader.Imperative.cs`, `tests/DataverseSolutionCompiler.UnitTests/CodeFirstSdkRegistrationReaderTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/CompilerKernelTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/CodeAssetBuilderTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/CliApplicationTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/IntentSpecCompilerTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/PackageEmitterTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/StableOverlapDriftComparerTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/WebApiLiveSnapshotProviderTests.cs`, `tests/DataverseSolutionCompiler.IntegrationTests/TrackedSourceEmitterIntegrationTests.cs`, `tests/DataverseSolutionCompiler.E2ETests/CodeFirstPluginWorkflowProofTests.cs`, `fixtures/skill-corpus/examples/seed-code-plugin-helper`, `fixtures/skill-corpus/examples/seed-code-plugin-imperative-service` | The compiler now covers one more real family of DBM-style code-first registration shapes without broadening into arbitrary helper inference. Zero-argument helper-returned registration collections now work for supported `Types`, `Steps`, and `Images` arrays, the more realistic imperative `GetMessage(service, entity, message, handler)` shape now resolves by its stable trailing context arguments, helper-backed mixed normal plug-in plus `customWorkflowActivity` type catalogs now survive tracked-source, reverse generation, classic staged build, live readback, and stable-overlap, and the owner-level `Workflow` boundary still remains unchanged. |
| Code-extensibility and workflow closure | done | `src/DataverseSolutionCompiler.Readers.Code/CodeFirstSdkRegistrationReader.cs`, `src/DataverseSolutionCompiler.Readers.Code/CodeFirstSdkRegistrationReader.Imperative.cs`, `src/DataverseSolutionCompiler.Readers.Xml/XmlCanonicalSolutionParser.Workflows.cs`, `src/DataverseSolutionCompiler.Readers.Live/DataverseWebApiLiveReader.ProcessSecurity.cs`, `src/DataverseSolutionCompiler.Emitters.TrackedSource/TrackedSourceEmitter.cs`, `src/DataverseSolutionCompiler.Emitters.Package/PackageEmitter.cs`, `tests/DataverseSolutionCompiler.UnitTests/CodeFirstSdkRegistrationReaderTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/PackageEmitterTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/WebApiLiveSnapshotProviderTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/StableOverlapDriftComparerTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/XmlSolutionReaderTests.cs`, `tests/DataverseSolutionCompiler.IntegrationTests/TrackedSourceEmitterIntegrationTests.cs`, `fixtures/skill-corpus/examples/seed-workflow-classic`, `fixtures/skill-corpus/examples/seed-workflow-action` | The code-first parser now supports a broader bounded common-idiom lane without crossing into arbitrary program execution, regular plug-in packages now stay explicit live finalize-apply rather than fake solution-zip parity, custom workflow activity package deployment is now a cited permanent classic-only boundary across build and workflow surfaces, and the owner `Workflow` lane is reopened for a curated classic workflow and custom-action subset through source-backed parse, tracked-source summaries, reverse-generated `sourceBackedArtifacts[]`, package-input emission, live readback, and stable-overlap drift. |
| .NET 10 solution skeleton | done | `DataverseSolutionCompiler.sln`, `global.json`, `Directory.Build.props`, `Directory.Packages.props`, `src/`, `tests/` | The repo builds on `net10.0` and matches the approved product shape. |
| Typed compiler contracts | done | `src/DataverseSolutionCompiler.Domain` | Canonical IR, diagnostics, requests/results, and public interfaces now exist in C#. |
| Compiler core and CLI spine | done | `src/DataverseSolutionCompiler.Compiler`, `src/DataverseSolutionCompiler.Cli`, `tests/DataverseSolutionCompiler.UnitTests/CompilerKernelTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/CliApplicationTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/IntentSpecCompilerTests.cs` | Capability registry, planner, explanation service, real source-backed kernel reads, compiler-native JSON intent reads, tracked-source subset reads, and the operational CLI surface including `emit --layout intent-spec` are implemented for the current proven slice. Classic exported solution zips now flow directly into reverse-generated intent-spec output without a manual `pac solution unpack` step, the intent surface now also supports hybrid `sourceBackedArtifacts[]` references for broader rebuildable families, and `apply-dev` now runs the verified `compile -> apply -> readback -> diff` flow for the current supported live-mutation family set. |
| Structural source reader proof | partial | `src/DataverseSolutionCompiler.Readers.Xml`, `tests/DataverseSolutionCompiler.UnitTests/XmlSolutionReaderTests.cs`, `fixtures/skill-corpus/examples/seed-*` | The XML reader now parses typed artifacts for the strongest proven source families: publisher, solution shell, tables, authored columns, relationships, option sets, alternate keys, image configurations, forms, views, visualizations, app modules, app settings, site maps, web resources, compact ribbon shell metadata from `Entities/<entity>/RibbonDiff.xml`, environment variables, canvas apps, the source-first import-map slice with child data-source mappings, a compact entity-analytics slice, a compact AI slice covering `AI Project Type`, `AI Project`, and `AI Configuration`, a neutral plugin-registration slice covering `PluginAssembly`, `PluginType`, `PluginStep`, and `PluginStepImage` from `Other/Customizations.xml` plus packaged assembly payload evidence, a neutral integration-endpoint slice covering `ServiceEndpoint` plus `Connector`, a process-policy slice covering duplicate-rule, routing-rule, and mobile-offline rule shells plus child items, the reopened workflow subset from `Workflows/*.json` plus `.xaml`, a security-definition slice covering role shell, field-security shell, field permission, and connection role, and explicit source-first `SimilarityRule`, `Sla`, and `SlaItem` parsing from `Other/Customizations.xml`. Narrow managed-property proof now flows through stable `isCustomizable` flags on owning table and column artifacts. Later families remain partial. |
| JSON intent reader and greenfield generator v1 | partial | `src/DataverseSolutionCompiler.Readers.Intent`, `src/DataverseSolutionCompiler.Emitters.Package`, `tests/DataverseSolutionCompiler.UnitTests/IntentSpecCompilerTests.cs`, `fixtures/intent-specs/seed-greenfield-v1.json` | The compiler can now consume a compiler-native JSON v1 intent surface, project it into the shared canonical IR, emit tracked-source, reverse-generate normalized intent-spec output back from the supported tracked-source subset, synthesize PAC-packable unpacked solution input for the supported v1 families, and round-trip back through `XmlSolutionReader` plus stable-overlap diff. The intent surface now also supports hybrid `sourceBackedArtifacts[]` entries for broader rebuildable families, image columns plus primary-image binding and narrow `isCustomizable`, supported `main` / `quick` / `card` forms with `field`, `quickView`, and `subgrid` controls, and structured authored-chart metadata for rebuild-safe saved-query visualizations when the source metadata is complete. |
| Tracked-source reverse generation to intent-spec | partial | `src/DataverseSolutionCompiler.Readers.TrackedSource`, `src/DataverseSolutionCompiler.Emitters.TrackedSource`, `src/DataverseSolutionCompiler.Readers.Xml/ZipSolutionReader.cs`, `tests/DataverseSolutionCompiler.UnitTests/IntentSpecCompilerTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/CliApplicationTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/XmlSolutionReaderTests.cs` | The compiler can now reconstruct the supported canonical subset from emitted tracked-source JSON and write one deterministic `intent-spec/intent-spec.json` document plus `intent-spec/reverse-generation-report.json`. Preserved form and view IDs now round-trip through the reverse-authoring surface. Classic export zips also reverse-generate directly after internal PAC unpack normalization. Reverse generation now extends beyond the original structured subset: broader rebuildable families can materialize into `sourceBackedArtifacts[]`, the report explicitly distinguishes unsupported families, unsupported shapes, platform-generated artifacts, missing source fidelity, and staged source-backed artifacts, supported quick/card/control-rich forms now reverse-generate structurally while missing-fidelity form variants fall back explicitly instead of disappearing, rebuild-safe authored charts now reverse-generate structurally when chart metadata is complete, supported app-shell detail such as `appSettings`, `roleIds`, plus `entity` / `url` / `webResource`, canonical `dashboard`, and canonical `customPage` site-map subareas survives classic-export reverse generation without being flattened into generic omissions, compact ribbon shell metadata now reverse-generates as explicit `sourceBackedArtifacts[]` instead of disappearing, the reopened workflow subset now reverse-generates into source-backed workflow JSON/XAML entries instead of dropping workflow shell fidelity, and representative compact environment, process-policy, security-definition, advanced-UI, and workflow seeds now also clear the direct classic-export reverse-generation path. |
| Wave 1 image rebuild proof | done | `src/DataverseSolutionCompiler.Apply/WebApiApplyExecutor.cs`, `src/DataverseSolutionCompiler.Cli/CliApplication.cs`, `src/DataverseSolutionCompiler.Readers.Xml/XmlCanonicalSolutionParser.ImageConfiguration.cs`, `tests/DataverseSolutionCompiler.UnitTests/XmlSolutionReaderTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/CliApplicationTests.cs`, `scratch/b010-wave1-live-proof/seed-image-config` | `seed-image-config` now clears the Wave 1 rebuild bar end to end. The derived image column path is stable enough to import, `publish` now performs a targeted post-import Dataverse metadata finalize step for `entityimageconfigs` plus `attributeimageconfigs`, live `diff` drops to non-blocking warnings only, and classic export ZIP reverse generation now recovers `primaryImageAttribute`, `canStoreFullImage`, and `isPrimaryImage` from `Other/Customizations.xml` instead of losing them. |
| Wave 2 advanced UI / broader app-shell rebuild proof | done | `src/DataverseSolutionCompiler.Readers.Xml/XmlCanonicalSolutionParser.Entities.cs`, `src/DataverseSolutionCompiler.Emitters.TrackedSource/IntentSpecEmitter.cs`, `src/DataverseSolutionCompiler.Emitters.TrackedSource/IntentSpecEmitter.Projection.cs`, `tests/DataverseSolutionCompiler.UnitTests/IntentSpecCompilerTests.cs`, `scratch/b010-wave2-live-proof/seed-advanced-ui` | `seed-advanced-ui` now clears the Wave 2 live export-backed rebuild loop. Reverse generation preserves the authored visualization structurally, stages the `account` table shell as source-backed `Entities/Account/Entity.xml` so system-table metadata is overlaid instead of regenerated, keeps supported app-shell detail (`appSettings`, `webResource` site-map subarea, environment variable, staged web-resource payloads), publishes successfully, and re-exports back into the same hybrid intent shape. |
| Wave 3 compact environment rebuild proof | done | `src/DataverseSolutionCompiler.Cli/CliApplication.cs`, `src/DataverseSolutionCompiler.Apply/WebApiApplyExecutor.cs`, `src/DataverseSolutionCompiler.Readers.Xml/XmlCanonicalSolutionParser.Environment.cs`, `tests/DataverseSolutionCompiler.UnitTests/CliApplicationTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/PackageEmitterTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/XmlSolutionReaderTests.cs`, `scratch/b010-wave3-live-proof/seed-environment`, `scratch/b010-wave3-live-proof/seed-entity-analytics` | `seed-environment` now clears the full live export/delete/rebuild/import/re-export loop for the packaged `CanvasApp` lane, and `seed-entity-analytics` now clears the same bar through the hybrid apply-only path. `publish` safely skips PAC import when the rebuilt package has no packageable root components, creates the solution shell directly, finalizes the entity analytics family through Web API, and the post-import export reverse-generates back into the same hybrid intent shape. |
| Wave 3 AI rebuild boundary | done | `src/DataverseSolutionCompiler.Apply/WebApiApplyExecutor.cs`, `src/DataverseSolutionCompiler.Readers.Live/DataverseWebApiLiveReader.Projection.cs`, `tests/DataverseSolutionCompiler.UnitTests/WebApiLiveSnapshotProviderTests.cs`, `scratch/b010-wave3-live-proof/seed-ai-families` | The compact AI seed remains reverse-generatable, package-rebuildable, and live-readable, but it is now closed as an explicit permanent boundary for the current environment. Real live create does not clear the bar honestly: `publish` now fails fast with a clear compiler diagnostic, and the underlying Dataverse `AITemplate` create surface returns `OperationNotSupported`, so this lane is no longer tracked as an unresolved partial inside `B-010`. |
| Wave 4 code/extensibility rebuild proof | done | `scratch/b010-wave4-live-proof/plugin-roundtrip`, `scratch/b010-wave4-live-proof/connector-scaffold-roundtrip`, `src/DataverseSolutionCompiler.Readers.Xml/XmlCanonicalSolutionParser.Extensibility.cs`, `src/DataverseSolutionCompiler.Apply/WebApiApplyExecutor.cs`, `tests/DataverseSolutionCompiler.UnitTests/IntentSpecCompilerTests.cs` | Wave 4 now clears the full live export-backed rebuild bar for the supported code/extensibility families. `seed-service-endpoint-connector` survives direct classic-export reverse generation, hybrid rebuild, PAC pack/import, and post-import re-export through the real connector (`372`) plus `PluginAssemblies/ServiceEndpoints.xml` source shape. `seed-plugin-registration` now does the same after sharded `SdkMessageProcessingSteps/*.xml` reverse generation started preserving `handlerPluginTypeName` plus `sdkMessageId`, allowing a real clean-delete proof where the plug-in assembly, type, step, and step image are recreated from reverse-generated intent and then re-export back into the same hybrid source-backed shape. |
| Wave 5 process and security rebuild proof | done | `scratch/b010-wave5-live-proof/seed-process-policy`, `scratch/b010-wave5-live-proof/seed-process-security` | `seed-process-policy` now clears the full live export/delete/rebuild/import/re-export loop for duplicate-rule, routing-rule, and mobile-offline families, and `seed-process-security` now does the same for role shell, role privilege, field-security profile or permission, and connection role. These supported definition families now have real live export-backed rebuild proof through the hybrid source-backed intent path, while effective access and the source-first `SimilarityRule` / `Sla` boundary remain out of scope. |
| Wave 6 permanent-boundary closure | done | `README.md`, `docs/roadmap.md`, `docs/backlog/backlog.md`, `docs/threads/current.md`, `fixtures/skill-corpus/references/component-coverage-matrix.md` | Import maps, child data-source mappings, compact AI, `SimilarityRule`, `Sla`, `SlaItem`, platform-generated views, effective-access expansion, and reporting/legacy are now documented consistently as explicit non-silent boundaries rather than implied rebuildability debt. |
| Wave 7 maximal supported proof | done | `scratch/b010-wave7-maximal-proof/final-proof-self-contained/rebuild-from-reverse-v4`, `scratch/b010-wave7-maximal-proof/final-proof-self-contained/post-reimport/`, `src/DataverseSolutionCompiler.Emitters.Package/PackageEmitter.cs`, `src/DataverseSolutionCompiler.Readers.Intent/IntentSpecReader.cs`, `tests/DataverseSolutionCompiler.UnitTests/IntentSpecCompilerTests.cs` | `CodexMetadataWave7Maximal` now clears the full program-end bar: classic export, direct reverse generation, delete of live solution and owned custom components, rebuild, PAC pack, publish, post-import export, and post-import reverse generation all succeed, and the final omission report contains only the expected platform-generated views plus the legacy customizations boundary. |
| B-007 reporting and legacy source-first slice | done | `fixtures/skill-corpus/examples/seed-reporting-legacy`, `src/DataverseSolutionCompiler.Readers.Xml/XmlCanonicalSolutionParser.ReportingLegacy.cs`, `src/DataverseSolutionCompiler.Emitters.TrackedSource/TrackedSourceEmitter.cs`, `src/DataverseSolutionCompiler.Readers.TrackedSource/TrackedSourceReader.cs`, `src/DataverseSolutionCompiler.Emitters.TrackedSource/IntentSpecEmitter.Projection.cs`, `src/DataverseSolutionCompiler.Emitters.Package/PackageEmitter.cs`, `src/DataverseSolutionCompiler.Diff/StableOverlapDriftComparer.cs`, `tests/DataverseSolutionCompiler.UnitTests/XmlSolutionReaderTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/PackageEmitterTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/IntentSpecCompilerTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/StableOverlapDriftComparerTests.cs`, `tests/DataverseSolutionCompiler.IntegrationTests/TrackedSourceEmitterIntegrationTests.cs`, `tests/DataverseSolutionCompiler.IntegrationTests/PacCliExecutorIntegrationTests.cs` | Reporting and legacy artifacts now have compact source-first proof for `Report`, `Template`, `DisplayString`, `Attachment`, and `LegacyAsset`: typed source parsing, deterministic tracked-source summaries, reverse generation into `sourceBackedArtifacts[]`, deterministic package preservation, and explicit non-blocking drift when honest live proof is absent. PAC pack is now also explicitly boundary-tested and fails with root-component validation against the compact synthetic seed, so this lane is closed honestly as source-first / permanent best-effort rather than overclaimed rebuild parity. |
| B-007 app-shell web-resource live slice | done | `src/DataverseSolutionCompiler.Readers.Live/DataverseWebApiLiveReader.cs`, `src/DataverseSolutionCompiler.Readers.Live/DataverseWebApiLiveReader.Families.cs`, `src/DataverseSolutionCompiler.Readers.Live/DataverseWebApiLiveReader.Http.cs`, `src/DataverseSolutionCompiler.Readers.Live/DataverseWebApiLiveReader.Projection.cs`, `tests/DataverseSolutionCompiler.UnitTests/WebApiLiveSnapshotProviderTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/StableOverlapDriftComparerTests.cs`, `fixtures/skill-corpus/examples/seed-app-shell/readback/artifacts/web-resources.json`, `fixtures/skill-corpus/examples/seed-advanced-ui/readback/artifacts/web-resources.json` | `WebResource` now clears the missing live-proof gap inside the app-shell lane without reopening broader app-shell scope. Solution-aware readback tracks component type `61`, projects `webresourceset` rows into `ComponentFamily.WebResource`, computes stable payload `byteLength` plus SHA-256 `contentHash` evidence from decoded content bytes, and keeps missing or undecodable content as explicit warning-only best effort instead of silently dropping the artifact. Fixture-backed overlap coverage now proves clean source/live comparison for both `seed-app-shell` and `seed-advanced-ui`. |
| B-007 app-module role-map reverse/package slice | done | `src/DataverseSolutionCompiler.Emitters.TrackedSource/IntentSpecEmitter.Models.cs`, `src/DataverseSolutionCompiler.Emitters.TrackedSource/IntentSpecEmitter.Projection.cs`, `src/DataverseSolutionCompiler.Readers.Intent/IntentSpecReader.cs`, `src/DataverseSolutionCompiler.Emitters.Package/PackageEmitter.Derived.cs`, `src/DataverseSolutionCompiler.Diff/StableOverlapDriftComparer.cs`, `tests/DataverseSolutionCompiler.UnitTests/IntentSpecCompilerTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/StableOverlapDriftComparerTests.cs`, `tests/DataverseSolutionCompiler.IntegrationTests/TrackedSourceEmitterIntegrationTests.cs` | App-module role access now survives the reverse/generator path without reopening broader live app-shell claims. Reverse generation carries structured `roleIds` in `appModules[]`, tracked-source summaries preserve role-map evidence, derived `AppModule.xml` rebuild writes `AppModuleRoleMaps`, and stable-overlap now records an explicit informational best-effort diagnostic when neutral live `appmodules` readback underreports `role_ids` instead of silently treating that gap as clean parity. |
| B-007 ribbon source-first slice | done | `src/DataverseSolutionCompiler.Readers.Xml/XmlCanonicalSolutionParser.Entities.cs`, `src/DataverseSolutionCompiler.Readers.Xml/XmlCanonicalSolutionParser.Ui.cs`, `src/DataverseSolutionCompiler.Emitters.TrackedSource/TrackedSourceEmitter.cs`, `src/DataverseSolutionCompiler.Readers.TrackedSource/TrackedSourceReader.cs`, `src/DataverseSolutionCompiler.Emitters.TrackedSource/IntentSpecEmitter.Projection.cs`, `src/DataverseSolutionCompiler.Diff/StableOverlapDriftComparer.cs`, `tests/DataverseSolutionCompiler.UnitTests/XmlSolutionReaderTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/PackageEmitterTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/IntentSpecCompilerTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/StableOverlapDriftComparerTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/WebApiLiveSnapshotProviderTests.cs`, `tests/DataverseSolutionCompiler.IntegrationTests/TrackedSourceEmitterIntegrationTests.cs` | Compact ribbon shell proof now closes honestly as source-first rather than staying ambiguous inside the broader app-shell lane. `RibbonDiff.xml` parses into typed `Ribbon` artifacts, tracked-source emits deterministic ribbon summaries, reverse generation carries the shell into `sourceBackedArtifacts[]`, package emission preserves deterministic `Entities/<entity>/RibbonDiff.xml` layout, `readback` reports ribbon as an explicit unsupported-live family, and stable-overlap keeps missing live ribbon artifacts non-blocking until new neutral evidence overturns that boundary. |
| B-007 standalone custom-control boundary slice | done | `src/DataverseSolutionCompiler.Domain/Model/CanonicalModel.cs`, `src/DataverseSolutionCompiler.Domain/Model/ArtifactPropertyKeys.cs`, `src/DataverseSolutionCompiler.Readers.Live/DataverseWebApiLiveReader.cs`, `src/DataverseSolutionCompiler.Readers.Live/DataverseWebApiLiveReader.Families.cs`, `src/DataverseSolutionCompiler.Readers.Live/DataverseWebApiLiveReader.Projection.cs`, `src/DataverseSolutionCompiler.Diff/StableOverlapDriftComparer.cs`, `tests/DataverseSolutionCompiler.UnitTests/WebApiLiveSnapshotProviderTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/StableOverlapDriftComparerTests.cs`, `fixtures/skill-corpus/examples/seed-advanced-ui/readback/artifacts/custom-controls.json` | Standalone `CustomControl` artifacts now have solution-scoped live projection through component type `66` plus `customcontrols`, with manifest/clientjson summaries recorded as stable comparison-safe properties. Because the neutral unmanaged export still omits matching standalone source artifacts, stable-overlap now records that lane as explicit source-asymmetric best effort instead of silent parity. `ComplexControl` and `CustomControlDefaultConfig` remain explicit no-row boundaries in the neutral corpus rather than hidden debt. |
| B-007 site-map definition live slice | done | `src/DataverseSolutionCompiler.Readers.Xml/XmlCanonicalSolutionParser.Apps.cs`, `src/DataverseSolutionCompiler.Readers.Intent/IntentSpecReader.cs`, `src/DataverseSolutionCompiler.Readers.Live/DataverseWebApiLiveReader.cs`, `src/DataverseSolutionCompiler.Readers.Live/DataverseWebApiLiveReader.Http.cs`, `src/DataverseSolutionCompiler.Readers.Live/DataverseWebApiLiveReader.Projection.cs`, `tests/DataverseSolutionCompiler.UnitTests/XmlSolutionReaderTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/WebApiLiveSnapshotProviderTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/StableOverlapDriftComparerTests.cs`, `fixtures/skill-corpus/examples/seed-app-shell/readback/readback.json`, `fixtures/skill-corpus/examples/seed-advanced-ui/readback/readback.json` | Site-map structure is no longer reduced to counts-only overlap. Source, intent, package reread, and live readback now all canonicalize supported site-map definitions into the same `SiteMapDefinitionJson`, including normalization of `$webresource:` subareas into the structured `webResource` field. Stable-overlap now detects navigation-shape drift through the site-map definition signature while the current neutral live proof remains narrowly grounded in the seeded web-resource-backed site-map shapes. |
| B-007 site-map adjunct fidelity slice | done | `src/DataverseSolutionCompiler.Readers.Xml/XmlCanonicalSolutionParser.Apps.cs`, `src/DataverseSolutionCompiler.Readers.Live/DataverseWebApiLiveReader.Http.cs`, `src/DataverseSolutionCompiler.Emitters.TrackedSource/IntentSpecEmitter.Models.cs`, `src/DataverseSolutionCompiler.Emitters.TrackedSource/IntentSpecEmitter.Projection.cs`, `src/DataverseSolutionCompiler.Readers.Intent/IntentSpecReader.cs`, `src/DataverseSolutionCompiler.Emitters.Package/PackageEmitter.Derived.cs`, `tests/DataverseSolutionCompiler.UnitTests/XmlSolutionReaderTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/WebApiLiveSnapshotProviderTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/StableOverlapDriftComparerTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/IntentSpecCompilerTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/PackageEmitterTests.cs` | Current neutral site-map seeds already carried explicit subarea adjunct detail such as `Icon` / `VectorIcon` and `Client` / `PassParams` / `AvailableOffline`, but the compiler was still flattening that detail away during reverse/package round-trip. Source parsing, reverse-generated intent, package rebuild, live readback, and stable-overlap now all preserve those seeded adjunct fields instead of silently normalizing them out. This closes the current seeded adjunct shape only; broader richer site-map target families remain later app-shell work. |
| B-007 site-map dashboard target slice | done | `src/DataverseSolutionCompiler.Readers.Xml/XmlCanonicalSolutionParser.Apps.cs`, `src/DataverseSolutionCompiler.Readers.Live/DataverseWebApiLiveReader.Http.cs`, `src/DataverseSolutionCompiler.Emitters.TrackedSource/IntentSpecEmitter.Models.cs`, `src/DataverseSolutionCompiler.Emitters.TrackedSource/IntentSpecEmitter.Projection.cs`, `src/DataverseSolutionCompiler.Readers.Intent/IntentSpecReader.cs`, `src/DataverseSolutionCompiler.Emitters.Package/PackageEmitter.Derived.cs`, `tests/DataverseSolutionCompiler.UnitTests/XmlSolutionReaderTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/WebApiLiveSnapshotProviderTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/StableOverlapDriftComparerTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/IntentSpecCompilerTests.cs` | The compiler now preserves the canonical GUID-backed site-map dashboard-target subset end to end instead of flattening it into generic URLs. Source parsing, reverse-generated intent, package rebuild, live readback, and stable-overlap all now carry a structured `dashboard` target when the XML uses the canonical `/main.aspx?pagetype=dashboard&id=<guid>` deep-link shape. Richer site-map targets such as custom pages and dashboard URLs with extra parameters remain explicit later app-shell work rather than being overclaimed by this slice. |
| B-007 site-map custom-page target slice | done | `src/DataverseSolutionCompiler.Readers.Xml/XmlCanonicalSolutionParser.Apps.cs`, `src/DataverseSolutionCompiler.Readers.Live/DataverseWebApiLiveReader.Http.cs`, `src/DataverseSolutionCompiler.Emitters.TrackedSource/IntentSpecEmitter.Models.cs`, `src/DataverseSolutionCompiler.Emitters.TrackedSource/IntentSpecEmitter.Projection.cs`, `src/DataverseSolutionCompiler.Readers.Intent/IntentSpecReader.cs`, `src/DataverseSolutionCompiler.Emitters.Package/PackageEmitter.Derived.cs`, `tests/DataverseSolutionCompiler.UnitTests/XmlSolutionReaderTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/WebApiLiveSnapshotProviderTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/StableOverlapDriftComparerTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/IntentSpecCompilerTests.cs` | The compiler now preserves the canonical site-map custom-page target subset end to end instead of flattening it into generic URLs. Source parsing, reverse-generated intent, package rebuild, live readback, and stable-overlap all now carry a structured `customPage` target when the XML uses `/main.aspx?pagetype=custom&name=<logicalName>`, with optional `appid=<guid>`. Richer custom-page URLs with extra parameters remain explicit later app-shell work rather than being overclaimed by this slice. |
| B-007 site-map Dataverse entity URL slice | done | `src/DataverseSolutionCompiler.Readers.Xml/XmlCanonicalSolutionParser.Apps.cs`, `src/DataverseSolutionCompiler.Readers.Live/DataverseWebApiLiveReader.Http.cs`, `src/DataverseSolutionCompiler.Emitters.TrackedSource/IntentSpecEmitter.Models.cs`, `src/DataverseSolutionCompiler.Emitters.TrackedSource/IntentSpecEmitter.Projection.cs`, `src/DataverseSolutionCompiler.Readers.Intent/IntentSpecReader.cs`, `src/DataverseSolutionCompiler.Emitters.Package/PackageEmitter.Derived.cs`, `tests/DataverseSolutionCompiler.UnitTests/XmlSolutionReaderTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/WebApiLiveSnapshotProviderTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/StableOverlapDriftComparerTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/IntentSpecCompilerTests.cs` | The compiler now preserves the canonical Dataverse entity deep-link subsets for site maps instead of flattening them into generic URLs. Source parsing, reverse-generated intent, package rebuild, live readback, and stable-overlap now carry app-aware `entitylist` deep links for specific views plus `entityrecord` deep links for specific records, with optional `appid`, optional `viewtype`, and narrow `extraqs=formid=<guid>` handling. Dashboard URLs with extra parameters, richer custom-page context parameters, and broader site-map target shapes remain explicit later app-shell work rather than being overclaimed by this slice. |
| B-007 site-map app-aware dashboard and custom-page context slice | done | `src/DataverseSolutionCompiler.Readers.Xml/XmlCanonicalSolutionParser.Apps.cs`, `src/DataverseSolutionCompiler.Readers.Live/DataverseWebApiLiveReader.Http.cs`, `src/DataverseSolutionCompiler.Emitters.TrackedSource/IntentSpecEmitter.Models.cs`, `src/DataverseSolutionCompiler.Emitters.TrackedSource/IntentSpecEmitter.Projection.cs`, `src/DataverseSolutionCompiler.Readers.Intent/IntentSpecReader.cs`, `src/DataverseSolutionCompiler.Emitters.Package/PackageEmitter.Derived.cs`, `tests/DataverseSolutionCompiler.UnitTests/XmlSolutionReaderTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/WebApiLiveSnapshotProviderTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/StableOverlapDriftComparerTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/IntentSpecCompilerTests.cs` | The compiler now preserves the next evidence-backed app-navigation subsets for site maps instead of flattening them into generic URLs. Source parsing, reverse-generated intent, package rebuild, live readback, and stable-overlap now preserve `dashboard` targets with optional `appId`, plus `customPage` targets with optional `customPageEntityName` and `customPageRecordId`, while broader arbitrary parameter-rich site-map URLs remain explicit later work rather than being overclaimed here. |
| B-007 site-map raw-url boundary slice | done | `src/DataverseSolutionCompiler.Readers.Xml/XmlCanonicalSolutionParser.Apps.cs`, `src/DataverseSolutionCompiler.Readers.Live/DataverseWebApiLiveReader.Http.cs`, `src/DataverseSolutionCompiler.Readers.Intent/IntentSpecReader.cs`, `tests/DataverseSolutionCompiler.UnitTests/XmlSolutionReaderTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/WebApiLiveSnapshotProviderTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/IntentSpecCompilerTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/StableOverlapDriftComparerTests.cs` | Unsupported richer site-map `main.aspx` links now stay explicit raw `url` evidence instead of ambiguous partial debt. Source parsing, reverse-generated intent, package rebuild, live readback, and stable-overlap now canonicalize raw URL parameter order, GUID forms, nested `extraqs`, and boolean literals so semantically equivalent richer links compare cleanly without overpromoting them into structured targets. |
| B-007 local option-set, quick/card form, and visualization live slice | done | `src/DataverseSolutionCompiler.Readers.Live/DataverseWebApiLiveReader.cs`, `src/DataverseSolutionCompiler.Readers.Live/DataverseWebApiLiveReader.Families.cs`, `src/DataverseSolutionCompiler.Readers.Live/DataverseWebApiLiveReader.Http.cs`, `src/DataverseSolutionCompiler.Readers.Live/DataverseWebApiLiveReader.Projection.cs`, `src/DataverseSolutionCompiler.Diff/StableOverlapDriftComparer.cs`, `tests/DataverseSolutionCompiler.UnitTests/WebApiLiveSnapshotProviderTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/StableOverlapDriftComparerTests.cs` | The current non-app-shell live/drift remainder now closes for three seeded lanes at once. Local picklist and boolean option sets now clear fixture-backed live stable-overlap instead of being drift-ignored, quick/card forms now clear strict entity-scoped live stable-overlap because solution-scope `systemforms` rows stay attached to the owning table, and solution-scoped saved-query visualizations now read back through component type `59` with normalized `datadescription` / `presentationdescription` signatures. System `state` / `status` option sets remain intentionally ignored, and the later audited closure now records user-owned or richer visualization breadth as an explicit supported-subset boundary instead of active backlog. |
| B-007 audited backlog closure | done | `fixtures/skill-corpus/references/solutioncomponent-componenttype-inventory.json`, `fixtures/skill-corpus/references/component-catalog.md`, `fixtures/skill-corpus/references/component-coverage-matrix.md`, `docs/backlog/backlog.md`, `docs/roadmap.md`, `docs/threads/current.md`, `README.md`, `tests/DataverseSolutionCompiler.UnitTests/ComponentUniverseInventoryTests.cs` | The formerly planned owner lanes `EntityMap`, `HierarchyRule`, and `ConvertRule` remain explicit best-effort boundaries because the repo still lacks neutral reusable source/live/package proof for them. The owner `Workflow` lane was later reopened under `B-017` for a curated source-backed subset. Richer user-owned visualization breadth and the remaining non-image schema-detail remainder are now explicit supported-subset boundaries instead of active backlog, the owner-family validation now prevents drift back to `planned`, and `B-007` is complete for the current audited universe. |
| Rebuild-fidelity hardening for supported authoring subset | done | `tests/DataverseSolutionCompiler.UnitTests/CliApplicationTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/IntentSpecCompilerTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/XmlSolutionReaderTests.cs`, `docs/threads/current.md` | Compact real export-backed loops now succeed through `export zip -> emit --layout intent-spec -> delete live solution and owned records -> pack -> import/publish` for both `DemoTableToolTest` and a richer `RebuildFidelityWave` probe. The supported subset now has live rebuild proof for solution shell, custom table, custom column, main form, rebuild-safe authored savedquery view, entity-only app module plus site map, and environment variable definition plus current value without manual unpack. |
| Tracked source emitter proof | partial | `src/DataverseSolutionCompiler.Emitters.TrackedSource`, `tests/DataverseSolutionCompiler.IntegrationTests/TrackedSourceEmitterIntegrationTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/CliApplicationTests.cs` | The tracked-source emitter writes a deterministic file tree, manifest inventory, entity/app shell JSON, entity-scoped `keys.json` alternate-key summaries, entity-scoped `image-configurations.json` summaries, compact `ribbons/<entity>.json` shell summaries, source-first import-map and data-source-mapping summaries, compact entity-analytics summaries, compact AI-family summaries, neutral plugin-registration summaries, neutral `service-endpoints/` and `connectors/` summaries, process-policy summaries for duplicate-rule, routing-rule, and mobile-offline families, reopened workflow summaries under `tracked-source/workflows/`, security-definition summaries for roles, field-security artifacts, and connection roles, source-first `similarity-rules/`, `slas/`, and `sla-items/` summaries, and copied web resource/canvas app package evidence for the strongest proven families. Form summaries are now disambiguated by type and id so same-name quick/card/main variants do not overwrite each other in tracked-source. Later families still remain partial. |
| Stable-overlap drift | partial | `src/DataverseSolutionCompiler.Diff`, `tests/DataverseSolutionCompiler.UnitTests/StableOverlapDriftComparerTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/WebApiLiveSnapshotProviderTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/IntentSpecCompilerTests.cs` | Comparison now uses family-aware stable properties for the strongest proven families and is exercised against fixture-backed live readback plus generated round-trip package rereads. It suppresses common live-only platform noise, generated legacy packaging noise, under-reported surfaces, source-first import-map families that now have an explicit permanent best-effort neutral boundary, compares alternate keys only on owning entity plus stable key name plus normalized key-attribute set while ignoring `EntityKeyIndexStatus`, compares image configuration only on entity plus scope plus image linkage plus stable image flags, compares narrow `isCustomizable` differences on tables and columns as blocking schema-detail drift, compares entity analytics only on the stable overlap, adds the same stable-overlap policy for the compact AI slice, compares `WebResource` on stable payload overlap (`webResourceType`, `byteLength`, `contentHash`) when live content bytes are available, compares site maps on canonical structured definition signatures including the current seeded subarea adjunct detail plus the canonical raw-`url` boundary for richer unsupported links instead of counts-only summaries, compares the reopened workflow subset on stable shell metadata plus XAML/client-data hashes and action metadata, keeps compact ribbon shell artifacts non-blocking when honest live parity is still unsupported, emits an explicit informational source-asymmetry diagnostic when standalone live `CustomControl` artifacts have no matching unmanaged source artifact, keeps the neutral plugin-registration slice free of runtime-only plugin operational noise, compares `ServiceEndpoint` plus `Connector` only on their durable shared transport and identity overlap, compares duplicate-rule, routing-rule, mobile-offline, role shell, field-security, and connection-role families on their stable owned-definition overlap, emits an explicit informational best-effort diagnostic when neutral live app-module readback underreports `role_ids`, keeps local picklist and boolean option sets in stable-overlap while leaving system `state` / `status` ignored as platform-owned operational surfaces, compares quick/card forms on the same stable signature as main forms instead of drift-ignoring them, compares solution-scoped saved-query visualizations on normalized chart-definition signatures, and keeps `SimilarityRule`, `Sla`, and `SlaItem` explicitly non-blocking when honest neutral live proof is absent. |
| Live readback | partial | `src/DataverseSolutionCompiler.Readers.Live`, `src/DataverseSolutionCompiler.Cli`, `tests/DataverseSolutionCompiler.UnitTests/WebApiLiveSnapshotProviderTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/CliApplicationTests.cs`, `fixtures/skill-corpus/examples/*/readback` | `Readers.Live` performs real Dataverse Web API projection with `DefaultAzureCredential` + `HttpClient`, OData paging, solution-aware discovery, entity-scoped form/view fallback, and typed projection into the shared canonical IR. CLI `readback` / `diff` now drive the real adapter for the same proven overlap, including solution-aware alternate-key readback through component type `14` plus `Keys(...)`, compact live-backed image configuration discovery through component types `431` and `432` with entity-scoped metadata fallback, narrow `isCustomizable` table/column projection, solution-scoped site-map definition readback through `sitemaps` plus canonical `SiteMapDefinitionJson` for the current seeded navigation shapes, including the seeded subarea `Icon` / `VectorIcon`, explicit `Client` / `PassParams` / `AvailableOffline` detail, the canonical GUID-backed dashboard subset plus the app-aware dashboard subset, the canonical custom-page subset plus custom-page record-context fields, the canonical Dataverse `entitylist` / `entityrecord` deep-link subsets, and the canonical raw-`url` boundary for richer unsupported site-map links, solution-scoped `WebResource` readback through component type `61` plus `webresourceset`, solution-scoped standalone `CustomControl` readback through component type `66` plus `customcontrols`, compact live-backed `entityanalyticsconfigs`, the AI-family slice, a neutral plugin-registration slice covering plugin assemblies, plugin types, sdk message processing steps, and step images, a neutral integration-endpoint slice covering `serviceendpoints` plus `connectors`, a process-policy slice covering duplicate-rule, routing-rule, and mobile-offline families with workflow or queue links kept as best-effort associations, the reopened owner-workflow subset through solution-scoped `workflows` projection, and a security-definition slice covering roles, field-security profiles, field permissions, and connection roles with role privileges kept definition-adjacent best effort. Strict solution-scope entity filtering now also keeps `systemforms`, `savedqueries`, and `savedqueryvisualizations` rows attached to the owning table, local picklist and boolean option sets now clear the same fixture-backed live overlap while system `state` / `status` remain intentionally ignored, and solution-scoped saved-query visualizations now read back through component type `59` with normalized `datadescription` / `presentationdescription` signatures. Import maps and data-source mappings remain an explicit permanent best-effort neutral boundary, ribbon is now also an explicit source-first unsupported-live family, standalone custom controls are now an explicit source-asymmetric best-effort boundary because unmanaged export omits matching source artifacts in the neutral corpus, app-module role-map parity is now also an explicit best-effort boundary because neutral `appmodules` readback underreports `role_ids`, `ComplexControl` and `CustomControlDefaultConfig` remain explicit no-row boundaries, and `SimilarityRule` and `Sla` or `SlaItem` remain explicit source-first unsupported-live families. |
| Package-input emission | partial | `src/DataverseSolutionCompiler.Emitters.Package`, `tests/DataverseSolutionCompiler.UnitTests/PackageEmitterTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/IntentSpecCompilerTests.cs`, `tests/DataverseSolutionCompiler.IntegrationTests/PacCliExecutorIntegrationTests.cs` | Package-input emission now supports deterministic source-first copying, derived XML synthesis for the supported JSON v1 generator families, and hybrid overlay for `sourceBackedArtifacts[]`. It writes an inventory manifest, preserves embedded alternate-key source in `Entities/<entity>/Entity.xml` for source-backed seeds, preserves embedded image-configuration source in the same entity XML shape, synthesizes the same proven key shape for JSON intent, keeps generator compatibility with stable `isCustomizable` projection on derived tables and columns, emits supported `main` / `quick` / `card` forms plus `field`, `quickView`, and `subgrid` controls into the correct FormXml folders, emits structured rebuild-safe visualization XML when chart metadata is present, emits derived `AppModuleRoleMaps` when reverse-generated app-module `roleIds` are present, preserves current seeded site-map adjunct detail such as subarea `Icon` / `VectorIcon` plus explicit `Client` / `PassParams` / `AvailableOffline` behavior, preserves the canonical GUID-backed site-map dashboard deep-link subset, the canonical custom-page deep-link subset, and the canonical raw-`url` boundary for richer unsupported links through derived `AppModuleSiteMap.xml`, preserves staged `Entities/<entity>/RibbonDiff.xml` layout for reverse-generated ribbon shells, preserves source-backed workflow JSON/XAML layout plus root component `29` for the reopened workflow subset, and now overlays staged web-resource payload files as well as `.data.xml` metadata during hybrid rebuild. It can also rebuild broader families such as visualizations, web resources, compact ribbon shells, canvas apps, entity analytics, compact AI families, neutral plugin-registration, neutral `ServiceEndpoints/` plus `Connectors/`, process-policy folders such as `duplicaterules/`, `RoutingRules/`, and `MobileOfflineProfiles/`, workflow folders under `Workflows/`, and security-definition folders such as `Roles/` plus `Other/FieldSecurityProfiles.xml` and `Other/ConnectionRoles.xml` from hybrid reverse-generated intent without silent loss. |
| PAC packaging/import and release orchestration | partial | `src/DataverseSolutionCompiler.Packaging.Pac`, `src/DataverseSolutionCompiler.Cli`, `tests/DataverseSolutionCompiler.UnitTests/PacCliExecutorTests.cs`, `tests/DataverseSolutionCompiler.IntegrationTests/PacCliExecutorIntegrationTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/CliApplicationTests.cs` | PAC pack/import/check now execute through real local process invocation with validated inputs, captured stdout/stderr diagnostics, CLI orchestration for `pack`, `import`, `publish`, and `check`, and conditional real-`pac` proof for both source-backed and generated package-inputs. That proof now includes reverse-generated intent from representative classic export ZIPs in the compact environment, process-policy, and security-definition lanes in addition to the earlier source-backed and generated package-input cases. Auth profile management, advanced import flags, and later-family package breadth remain intentionally out of scope. |
| Operational Dev apply and agent workflow v1 | done | `src/DataverseSolutionCompiler.Agent`, `src/DataverseSolutionCompiler.Cli`, `src/DataverseSolutionCompiler.Domain/Operations/WorkflowOperations.cs`, `tests/DataverseSolutionCompiler.UnitTests/AgentOrchestratorTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/CliApplicationTests.cs` | The reusable `AgentOrchestrator` workflow runner now owns the staged `compile -> apply -> readback -> diff` Dev-proof flow behind `apply-dev`. Domain workflow contracts carry stage outcomes plus aggregated diagnostics, `apply-dev` exits non-zero on apply/readback/blocking-drift failure, supported-scope no-op stays explicit success, and the v1 live-mutation scope remains intentionally frozen to the current `WebApiApplyExecutor` family set instead of implying broader agent autonomy or new live-mutable families. |
| Release workflow hardening and publish unification v1 | done | `src/DataverseSolutionCompiler.Agent`, `src/DataverseSolutionCompiler.Cli`, `src/DataverseSolutionCompiler.Domain/Operations/WorkflowOperations.cs`, `tests/DataverseSolutionCompiler.UnitTests/AgentOrchestratorTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/CliApplicationTests.cs` | The release-side workflow now uses the same library-first orchestration model as `apply-dev`. Package-build and publish workflow contracts now carry stage outcomes plus aggregated diagnostics, `pack` and `check` route through reusable `compile -> emit package-inputs -> pack` orchestration, `publish` routes through reusable `compile -> emit package-inputs -> pack -> import? -> finalize apply` orchestration, and the empty-package apply-only branch is now an explicit import-skipped state rather than hidden CLI branching. This phase keeps `publish` semantically unchanged and does not widen it into a broader verification command. |
| Seed fixture corpus | done | `fixtures/skill-corpus/manifest.json` and copied examples/references | The current Dataverse skill corpus is preserved locally as the initial regression and reference surface, now including dedicated compact alternate-key and image-config seeds alongside compact neutral import-map, entity-analytics, AI-family, plugin-registration, and service-endpoint/connector seeds plus the reusable `seed-process-policy`, `seed-process-security`, `source-only-similarity-rule`, and `source-only-sla` fixtures for process, security, and source-first service-policy proof. |

## Acceptance Notes

- Add a new row whenever a roadmap slice becomes evidence-backed.
- Keep the ledger short enough that a new thread can read it in one pass.
- If a family is still missing proof, say so plainly rather than inferring completion.
