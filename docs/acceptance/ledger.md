# Acceptance Ledger

This ledger records what is currently proven in the `.NET 10` repository, what is still partial, and what remains missing.

## Evidence Scale

- `done` - proven and documented.
- `partial` - useful proof exists, but boundaries remain.
- `missing` - not yet proven in a durable way.

## Ledger

| Area | Status | Evidence | Notes |
| --- | --- | --- | --- |
| Docs spine | done | `README.md`, `docs/roadmap.md`, `docs/architecture.md`, `docs/backlog/backlog.md`, `docs/acceptance/ledger.md`, `docs/threads/current.md` | Repository now has a stable planning and handoff surface. |
| Generator status accounting | done | `README.md`, `fixtures/skill-corpus/references/component-coverage-matrix.md`, `docs/roadmap.md`, `docs/backlog/backlog.md`, `docs/threads/current.md` | Docs now distinguish `Intent author`, `Reverse from tracked-source`, and `Rebuild from intent` readiness so "touched" no longer implies "fully generatable." The current classes are `full rebuildable`, `authorable but partial`, `source/readback only`, and `source-first / permanent best-effort`. |
| .NET 10 solution skeleton | done | `DataverseSolutionCompiler.sln`, `global.json`, `Directory.Build.props`, `Directory.Packages.props`, `src/`, `tests/` | The repo builds on `net10.0` and matches the approved product shape. |
| Typed compiler contracts | done | `src/DataverseSolutionCompiler.Domain` | Canonical IR, diagnostics, requests/results, and public interfaces now exist in C#. |
| Compiler core and CLI spine | done | `src/DataverseSolutionCompiler.Compiler`, `src/DataverseSolutionCompiler.Cli`, `tests/DataverseSolutionCompiler.UnitTests/CompilerKernelTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/CliApplicationTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/IntentSpecCompilerTests.cs` | Capability registry, planner, explanation service, real source-backed kernel reads, compiler-native JSON intent reads, tracked-source subset reads, and the operational CLI surface including `emit --layout intent-spec` are implemented for the current proven slice. Classic exported solution zips now flow directly into reverse-generated intent-spec output without a manual `pac solution unpack` step, and the intent surface now also supports hybrid `sourceBackedArtifacts[]` references for broader rebuildable families. |
| Structural source reader proof | partial | `src/DataverseSolutionCompiler.Readers.Xml`, `tests/DataverseSolutionCompiler.UnitTests/XmlSolutionReaderTests.cs`, `fixtures/skill-corpus/examples/seed-*` | The XML reader now parses typed artifacts for the strongest proven source families: publisher, solution shell, tables, authored columns, relationships, option sets, alternate keys, image configurations, forms, views, visualizations, app modules, app settings, site maps, web resources, environment variables, canvas apps, the source-first import-map slice with child data-source mappings, a compact entity-analytics slice, a compact AI slice covering `AI Project Type`, `AI Project`, and `AI Configuration`, a neutral plugin-registration slice covering `PluginAssembly`, `PluginType`, `PluginStep`, and `PluginStepImage` from `Other/Customizations.xml` plus packaged assembly payload evidence, a neutral integration-endpoint slice covering `ServiceEndpoint` plus `Connector`, a process-policy slice covering duplicate-rule, routing-rule, and mobile-offline rule shells plus child items, a security-definition slice covering role shell, field-security shell, field permission, and connection role, and explicit source-first `SimilarityRule`, `Sla`, and `SlaItem` parsing from `Other/Customizations.xml`. Narrow managed-property proof now flows through stable `isCustomizable` flags on owning table and column artifacts. Later families remain partial. |
| JSON intent reader and greenfield generator v1 | partial | `src/DataverseSolutionCompiler.Readers.Intent`, `src/DataverseSolutionCompiler.Emitters.Package`, `tests/DataverseSolutionCompiler.UnitTests/IntentSpecCompilerTests.cs`, `fixtures/intent-specs/seed-greenfield-v1.json` | The compiler can now consume a compiler-native JSON v1 intent surface, project it into the shared canonical IR, emit tracked-source, reverse-generate normalized intent-spec output back from the supported tracked-source subset, synthesize PAC-packable unpacked solution input for the supported v1 families, and round-trip back through `XmlSolutionReader` plus stable-overlap diff. The intent surface now also supports hybrid `sourceBackedArtifacts[]` entries for broader rebuildable families, image columns plus primary-image binding and narrow `isCustomizable`, supported `main` / `quick` / `card` forms with `field`, `quickView`, and `subgrid` controls, and structured authored-chart metadata for rebuild-safe saved-query visualizations when the source metadata is complete. |
| Tracked-source reverse generation to intent-spec | partial | `src/DataverseSolutionCompiler.Readers.TrackedSource`, `src/DataverseSolutionCompiler.Emitters.TrackedSource`, `src/DataverseSolutionCompiler.Readers.Xml/ZipSolutionReader.cs`, `tests/DataverseSolutionCompiler.UnitTests/IntentSpecCompilerTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/CliApplicationTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/XmlSolutionReaderTests.cs` | The compiler can now reconstruct the supported canonical subset from emitted tracked-source JSON and write one deterministic `intent-spec/intent-spec.json` document plus `intent-spec/reverse-generation-report.json`. Preserved form and view IDs now round-trip through the reverse-authoring surface. Classic export zips also reverse-generate directly after internal PAC unpack normalization. Reverse generation now extends beyond the original structured subset: broader rebuildable families can materialize into `sourceBackedArtifacts[]`, the report explicitly distinguishes unsupported families, unsupported shapes, platform-generated artifacts, missing source fidelity, and staged source-backed artifacts, supported quick/card/control-rich forms now reverse-generate structurally while missing-fidelity form variants fall back explicitly instead of disappearing, rebuild-safe authored charts now reverse-generate structurally when chart metadata is complete, and supported app-shell detail such as `appSettings` plus `entity` / `url` / `webResource` site-map subareas survives classic-export reverse generation without being flattened into generic omissions. |
| Rebuild-fidelity hardening for supported authoring subset | done | `tests/DataverseSolutionCompiler.UnitTests/CliApplicationTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/IntentSpecCompilerTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/XmlSolutionReaderTests.cs`, `docs/threads/current.md` | Compact real export-backed loops now succeed through `export zip -> emit --layout intent-spec -> delete live solution and owned records -> pack -> import/publish` for both `DemoTableToolTest` and a richer `RebuildFidelityWave` probe. The supported subset now has live rebuild proof for solution shell, custom table, custom column, main form, rebuild-safe authored savedquery view, entity-only app module plus site map, and environment variable definition plus current value without manual unpack. |
| Tracked source emitter proof | partial | `src/DataverseSolutionCompiler.Emitters.TrackedSource`, `tests/DataverseSolutionCompiler.IntegrationTests/TrackedSourceEmitterIntegrationTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/CliApplicationTests.cs` | The tracked-source emitter writes a deterministic file tree, manifest inventory, entity/app shell JSON, entity-scoped `keys.json` alternate-key summaries, entity-scoped `image-configurations.json` summaries, source-first import-map and data-source-mapping summaries, compact entity-analytics summaries, compact AI-family summaries, neutral plugin-registration summaries, neutral `service-endpoints/` and `connectors/` summaries, process-policy summaries for duplicate-rule, routing-rule, and mobile-offline families, security-definition summaries for roles, field-security artifacts, and connection roles, source-first `similarity-rules/`, `slas/`, and `sla-items/` summaries, and copied web resource/canvas app package evidence for the strongest proven families. Form summaries are now disambiguated by type and id so same-name quick/card/main variants do not overwrite each other in tracked-source. Later families still remain partial. |
| Stable-overlap drift | partial | `src/DataverseSolutionCompiler.Diff`, `tests/DataverseSolutionCompiler.UnitTests/StableOverlapDriftComparerTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/WebApiLiveSnapshotProviderTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/IntentSpecCompilerTests.cs` | Comparison now uses family-aware stable properties for the strongest proven families and is exercised against fixture-backed live readback plus generated round-trip package rereads. It suppresses common live-only platform noise, generated legacy packaging noise, under-reported surfaces, source-first import-map families that now have an explicit permanent best-effort neutral boundary, compares alternate keys only on owning entity plus stable key name plus normalized key-attribute set while ignoring `EntityKeyIndexStatus`, compares image configuration only on entity plus scope plus image linkage plus stable image flags, compares narrow `isCustomizable` differences on tables and columns as blocking schema-detail drift, compares entity analytics only on the stable overlap, adds the same stable-overlap policy for the compact AI slice, keeps the neutral plugin-registration slice free of runtime-only plugin operational noise, compares `ServiceEndpoint` plus `Connector` only on their durable shared transport and identity overlap, compares duplicate-rule, routing-rule, mobile-offline, role shell, field-security, and connection-role families on their stable owned-definition overlap, and keeps `SimilarityRule`, `Sla`, and `SlaItem` explicitly non-blocking when honest neutral live proof is absent. |
| Live readback | partial | `src/DataverseSolutionCompiler.Readers.Live`, `src/DataverseSolutionCompiler.Cli`, `tests/DataverseSolutionCompiler.UnitTests/WebApiLiveSnapshotProviderTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/CliApplicationTests.cs`, `fixtures/skill-corpus/examples/*/readback` | `Readers.Live` performs real Dataverse Web API projection with `DefaultAzureCredential` + `HttpClient`, OData paging, solution-aware discovery, entity-scoped form/view fallback, and typed projection into the shared canonical IR. CLI `readback` / `diff` now drive the real adapter for the same proven overlap, including solution-aware alternate-key readback through component type `14` plus `Keys(...)`, compact live-backed image configuration discovery through component types `431` and `432` with entity-scoped metadata fallback, narrow `isCustomizable` table/column projection, compact live-backed `entityanalyticsconfigs`, the AI-family slice, a neutral plugin-registration slice covering plugin assemblies, plugin types, sdk message processing steps, and step images, a neutral integration-endpoint slice covering `serviceendpoints` plus `connectors`, a process-policy slice covering duplicate-rule, routing-rule, and mobile-offline families with workflow or queue links kept as best-effort associations, and a security-definition slice covering roles, field-security profiles, field permissions, and connection roles with role privileges kept definition-adjacent best effort. Import maps and data-source mappings remain an explicit permanent best-effort neutral boundary, `SimilarityRule` and `Sla` or `SlaItem` remain explicit source-first unsupported-live families, and quick/card forms, some local option sets, visualizations, web resources, and role-map detail remain partial. |
| Package-input emission | partial | `src/DataverseSolutionCompiler.Emitters.Package`, `tests/DataverseSolutionCompiler.UnitTests/PackageEmitterTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/IntentSpecCompilerTests.cs`, `tests/DataverseSolutionCompiler.IntegrationTests/PacCliExecutorIntegrationTests.cs` | Package-input emission now supports deterministic source-first copying, derived XML synthesis for the supported JSON v1 generator families, and hybrid overlay for `sourceBackedArtifacts[]`. It writes an inventory manifest, preserves embedded alternate-key source in `Entities/<entity>/Entity.xml` for source-backed seeds, preserves embedded image-configuration source in the same entity XML shape, synthesizes the same proven key shape for JSON intent, keeps generator compatibility with stable `isCustomizable` projection on derived tables and columns, emits supported `main` / `quick` / `card` forms plus `field`, `quickView`, and `subgrid` controls into the correct FormXml folders, emits structured rebuild-safe visualization XML when chart metadata is present, and now overlays staged web-resource payload files as well as `.data.xml` metadata during hybrid rebuild. It can also rebuild broader families such as visualizations, web resources, canvas apps, entity analytics, compact AI families, neutral plugin-registration, neutral `ServiceEndpoints/` plus `Connectors/`, process-policy folders such as `duplicaterules/`, `RoutingRules/`, and `MobileOfflineProfiles/`, and security-definition folders such as `Roles/` plus `Other/FieldSecurityProfiles.xml` and `Other/ConnectionRoles.xml` from hybrid reverse-generated intent without silent loss. |
| PAC packaging/import and release orchestration | partial | `src/DataverseSolutionCompiler.Packaging.Pac`, `src/DataverseSolutionCompiler.Cli`, `tests/DataverseSolutionCompiler.UnitTests/PacCliExecutorTests.cs`, `tests/DataverseSolutionCompiler.IntegrationTests/PacCliExecutorIntegrationTests.cs`, `tests/DataverseSolutionCompiler.UnitTests/CliApplicationTests.cs` | PAC pack/import/check now execute through real local process invocation with validated inputs, captured stdout/stderr diagnostics, CLI orchestration for `pack`, `import`, `publish`, and `check`, and conditional real-`pac` proof for both source-backed and generated package-inputs. Auth profile management, advanced import flags, and later-family package breadth remain intentionally out of scope. |
| Agent orchestration | partial | `src/DataverseSolutionCompiler.Agent` | Compiler-backed orchestration exists as a bootstrap entry point, not yet a full autonomous workflow layer. |
| Seed fixture corpus | done | `fixtures/skill-corpus/manifest.json` and copied examples/references | The current Dataverse skill corpus is preserved locally as the initial regression and reference surface, now including dedicated compact alternate-key and image-config seeds alongside compact neutral import-map, entity-analytics, AI-family, plugin-registration, and service-endpoint/connector seeds plus the reusable `seed-process-policy`, `seed-process-security`, `source-only-similarity-rule`, and `source-only-sla` fixtures for process, security, and source-first service-policy proof. |

## Acceptance Notes

- Add a new row whenever a roadmap slice becomes evidence-backed.
- Keep the ledger short enough that a new thread can read it in one pass.
- If a family is still missing proof, say so plainly rather than inferring completion.
