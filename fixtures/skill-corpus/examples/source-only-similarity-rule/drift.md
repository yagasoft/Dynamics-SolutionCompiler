# Similarity Rule Readback Boundary

- This example is intentionally source-only.
- No live readback snapshot is bundled for it.
- Use `inventory.md` and `normalized/` to study the supported source shape for `SimilarityRule`.
- If a target environment exposes a specific `similarityrule` record id, the global skill may still attempt best-effort record-id-based readback, but that is not part of this bundled example.
