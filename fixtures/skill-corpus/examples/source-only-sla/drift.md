# SLA Readback Boundary

- This example is intentionally source-only.
- No live readback snapshot is bundled for it.
- Use `inventory.md` and `normalized/` to study the supported source shape for `SLA` and `SLAItem`.
- It exists because the current environment still needs an SLA-enabled neutral table before a live unattended seed can be represented honestly.
