# DBM Baseline Example

This is an optional DBM-specific reference derived from:
- `C:\Git\Dynamics-BusinessMachine\power-platform\solutions\DynamicsBusinessMachine\baseline`

Use it only when a task explicitly benefits from DBM context.

What it is useful for:
- understanding a tracked baseline solution that is packaging-oriented rather than entity-heavy
- seeing how DBM separates tracked baseline metadata from later generated metadata layers
- pairing with DBM ALM references when choosing between direct Dev proof and durable packaged release outputs
- pairing with `references/examples/dbm-sdk-registration/` when you need the source-first sdk-message and plugin-step registration layer that is not fully visible in the tracked baseline export

What it is not:
- a universal Dataverse rule set
- a neutral form or schema example
- the default baseline for non-DBM projects
