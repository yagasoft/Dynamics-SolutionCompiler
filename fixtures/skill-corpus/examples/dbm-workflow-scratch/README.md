# DBM Workflow Scratch

This optional DBM-specific example stays outside the neutral seed corpus.

Use it for:
- studying real unpacked workflow source without treating DBM as the global baseline
- seeing how DBM solution artifacts combine app shell, web resources, and workflow XAML in one project-specific slice
- inspecting richer embedded FormXML `controlDescriptions` through `account-form-summary.md` and `account-form-summary.json`

Important limits:
- this is a project-specific example, not a neutral seed
- use it to understand source shape and advanced embedded-control analysis, not to import DBM assumptions into general Dataverse guidance
