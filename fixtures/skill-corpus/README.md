# Skill Corpus Snapshot

This folder snapshots the current `dataverse-metadata-synthesis` skill references and example corpus into the compiler knowledge base.

## Source Lineage

Imported from:

- `C:\Users\Ahmed Elsawalhy\.codex\skills\dataverse-metadata-synthesis\references\`

Snapshot intent:

- preserve the current known-good Dataverse family guidance
- seed the compiler roadmap, backlog, and acceptance model
- provide immediate fixture input for early .NET readers and drift tests

## Notes

- `references/` contains the documentation snapshot used as the seed spec.
- `examples/` contains neutral and optional DBM-flavored evidence corpora.
- This snapshot is a starting point, not the permanent final corpus layout for the compiler.
